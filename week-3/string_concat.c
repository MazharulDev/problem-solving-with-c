#include<stdio.h>
#include<stdio.h>

int main()
{
    char a[101], b[101];
    
    scanf("%s %s",&a,&b);

    char tmp=a;

    int a_length=strlen(a);
    int b_length=strlen(b);

    for(int i=0;i<a_length+a_length;i++){
          
    }
}